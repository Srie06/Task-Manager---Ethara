{
  "name": "task-manager-ethara",
  "version": "1.0.0",
  "description": "Monorepo root — runs backend and frontend concurrently",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node start.js"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
